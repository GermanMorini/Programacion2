#include<iostream>

using namespace std;

void ritrevni(char s[50], int l) {
    for (char *i = &s[0]; i < &s[0] + l; i++) {
        char tmp = s[&s[l-1] - i]
    }
}

int main() {
    
    return 0;
}